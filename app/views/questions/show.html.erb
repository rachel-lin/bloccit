<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>
<%= button_to "Delete", question_path(@question.id), :method=>:delete, class: 'btn btn-success' %>
<p><%= @question.body %></p>